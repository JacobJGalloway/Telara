@page "/workflow"
@attribute [Authorize]

<PageTitle>Workflow</PageTitle>

<h1>Workflow</h1>

@* Hardcoded to the single-line demo (FAC-001, ST-INTAKE -> ST-ASSEMBLY -> ST-PACK -> ST-DOCK,
   seeded via 003.SeedStationRouting-Demo.sql) for now - a line/station selector lands once the
   multi-line domain model exists to pick one from, same reasoning as Dashboard.razor's
   hardcoded demo-facility/station-01. *@
<StationFlowDiagram FacilityId="FAC-001" StationId="ST-INTAKE" />
