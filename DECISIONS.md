# Architectural Decisions

Persistent record of resolved architectural decisions and their rationale. Unlike `ARCHITECTURE.md` (deleted before each merge to `main`, since it holds sprint-scoped goals/non-goals/definition-of-done), this file survives merges — it's the "why" behind the stack that isn't reconstructable from reading the code alone.

Each entry keeps the date it was resolved. New decisions get appended; nothing here gets deleted at sprint boundaries.

---

## Messaging / Transport — 2026-07-22
Message queue is **Apache Kafka**, self-hosted on-prem for now (single broker, KRaft mode — no separate ZooKeeper), with **Azure Event Hubs' Kafka-compatible endpoint** as the eventual managed target once budget/scale justify it. Decision drivers:
- Volume is not the concern — even a full-plant reconnect burst (10 stations × ~10 pieces of equipment each, emitting on a 15-minute interval to start) is on the order of 100 messages, far under Kafka's comfortable range. The concern is decoupling ingestion from the synchronous API path so a burst doesn't stall response times or hammer SQL Server with concurrent writes.
- The multi-level consumer pattern the analytics roles need (station supervisor → equipment-level, shift manager → station-level, plant manager → part/sub-assembly-level) maps naturally onto Kafka's log model: each role's consumer group tails/replays the same topic independently at its own pace, without one consumer's read affecting another's. This is a real advantage over RabbitMQ's queue model, which would need a fanout exchange plus one queue per consumer type to approximate the same thing.
- Kafka is free/OSS to self-host, and Event Hubs speaks the Kafka wire protocol — so today's .NET producer and Java (Spring Kafka) consumer code carries forward unchanged when the org eventually migrates to the managed service; only the broker connection config changes. This follows the same on-prem-now/Azure-compatible-later pattern used for SQL Server 2025 Express → Azure SQL.
- Retention on Kafka topics should stay short (hours to a few days) — SQL Server remains the system of record; Kafka is a transient buffer/replay window, not long-term storage.
- Partition key should be `EquipmentId` (or `StationId`+`EquipmentId`) rather than `StationId` alone, to preserve per-device ordering and to match the smaller, equipment-scoped domain model (readings originate from station equipment, not the station itself) that keeps API response times fast.
- Schema Registry (Confluent's OSS Schema Registry, self-hosted) is deferred until there are actual cross-language producers/consumers sharing a topic — expected once Escalation & Notification (Java/Spring) is wired in.

Confirmed pattern: Escalation & Notification is one of potentially several fan-out subscribers on a shared publish event — e.g., a detected issue publishes once, and the SQL persistence path and the Escalation & Notification push path each subscribe independently and run in parallel, with no dependency between them.

## Identity & Multi-Facility — 2026-07-22
`FacilityId` is stubbed into the compound key now (`Station` PK, `TelemetryKey`) so the schema doesn't need a retrofit if multi-facility is ever built. That covers the data-model foundation only.

Full multi-facility *operation* is a larger extension than the key alone and stays explicitly out of scope until Phase 5 (if it happens at all — Phase 5 sits past the demo-stable pause point for pilot feedback, and was included partly to demonstrate domain awareness rather than as a committed build target). In particular, running from a centralized data center instead of a plant-level control room raises questions the schema doesn't answer: cross-facility aggregation/rollup views, network latency and connectivity assumptions between facilities and the central system, and whether write-lease semantics (`ActiveInstanceId`/`LeaseExpiresAt`) still hold the same way across a WAN instead of a plant LAN. Flag these for actual Phase 5 planning rather than resolving now.

## AI / RAG — 2026-07-22
**Vector storage:** SQL Server 2025's native `VECTOR` type, in the same database/tables as the relational fallback data — no separate vector database. This gets DiskANN-based approximate-nearest-neighbor indexing and `VECTOR_DISTANCE()` similarity queries, giving practical parity with pgvector's HNSW/IVFFlat + distance-operator approach, without a second data store to keep in sync.

**Collapse mechanism:** because vectors and relational fallback data live in the same store, the vector-match-then-fallback check is a single atomic query (CTE or stored proc) — run the vector search, check the top match's distance against a threshold, fall back to the relational lookup in the same round trip if nothing clears it. One hop, not an application-layer two-step check.

**Embedding generation:** not yet decided which model/service produces the embeddings written into the `VECTOR` column — but the call is routed through the existing **AI Abstraction Interface/Adapter** (already named in README's stack) rather than called directly from domain code. This means the current implementation (local/on-prem, TBD) can be swapped for an Azure OpenAI embeddings implementation later without touching any caller — same on-prem-now/Azure-compatible-later shape as SQL Server and Kafka.

**Two distinct embedding lifecycles** (pattern confirmed against a working prior implementation — see Prior Art note below):
- *Persisted embeddings* — generated and written to the `VECTOR` column when an aggregate/summary row is upserted (e.g., a shift/batch close). This is the "maintain" half: embeddings regenerate whenever the underlying aggregate changes, not once and left stale. Natural trigger point once Telara's own shift-based compaction job exists.
- *Ephemeral query-time embeddings* — generated fresh, in memory, for the *current* context at the point a reasoning step needs similarity search; used only as the search input, never persisted. No separate table or lifecycle needed for these — they exist for the duration of one query.

**Text-before-embedding construction:** embed a natural-language description built from structured fields (equipment type, station role, reading type, values), not raw numeric telemetry directly. This is what lets semantic similarity capture categorical/contextual meaning (e.g., a reading pattern on a stamping press vs. the same numeric pattern on a conveyor motor should not be treated as similar just because the numbers are close).

**Query-time interceptor location — still open, deliberately out of scope for Branch 1.1:** the ReAct-style loop that would decide "I need similarity search now" and generate the ephemeral embedding is a **tool orchestrator**, not the system orchestrator — a distinct layer from MAF. MAF is the top-level orchestrator (per README's diagram) and owns state management and other system-level concerns; ReAct-style tool selection/invocation reasoning is a narrower concern sitting closer to (or inside) the MCP tool layer. Leaning candidate: expose it as its own MCP tool (e.g. `find_similar_readings`), kept in the same MCP server as the main process rather than split into a separate server — undecided/worth a real debate once orchestration work starts.

**Open strategic question — is a vector/RAG pipeline worth building at all:** the broader RAG-via-embeddings approach may be losing ground industry-wide — some teams achieve the same "collapsed context" result (grounded, non-hallucinated answers) by having an agent run a linear sequence of ordinary tool calls instead of a vector-search step. Worth revisiting before investing further in the `VECTOR` column / embedding-generation infrastructure above: could Telara's Collapsed RAG goal be met by a linear tool-calling agent over the existing relational/EF Core data, without embeddings at all? Not resolved — flagging so the vector-infrastructure work isn't treated as a foregone conclusion.

**Prior art:** `vector_service.py` in the Yearly Yields project (Python/PostgreSQL/pgvector) is the closest existing implementation of this pattern — worth reviewing directly when this is picked back up, including its `find_similar_weeks` query shape (relational pre-filter on foreign keys + `embedding IS NOT NULL`, then `ORDER BY cosine distance LIMIT n`) and its job-triggered `upsert_historical_summary`. One flag before porting the embedding-generation call shape specifically: that file calls `anthropic.AsyncAnthropic().embeddings.create(model="voyage-3", ...)`, and it's unconfirmed whether the Anthropic SDK actually exposes an embeddings endpoint that way (embeddings are typically served through Voyage AI's own SDK/service, not the Anthropic client) — verify this actually hits a real endpoint before modeling the .NET embedding-service call the same way.

**Confidence threshold:** per-query-type, not a single global cutoff — a lookup table maps context/query type to its required confidence level (e.g. defect lookup vs. equipment troubleshooting may need different minimum similarity), rather than tuning one fixed number for every use case.

**LLM-unavailability degraded mode:** substantially de-risked by the above — the vector match and relational fallback both execute inside SQL Server and don't call the reasoning LLM (Claude Sonnet 5) to produce an answer. The external LLM is a downstream consumer of an already-resolved lookup, not a dependency of the lookup itself, so floor monitoring/telemetry queries aren't blocked by LLM API availability. (Embedding generation at write time is a separate dependency — revisit once the embedding model/service is chosen.)

## Ingestion / Hardware — 2026-07-22
Real sensor/PLC wire protocol (OPC-UA, Modbus, MQTT, vendor-specific) is intentionally **not** being decided yet. Ingestion is built behind an **adapter pattern** — raw device data goes through an ETL adapter into domain models before persistence — so whichever protocol real hardware eventually speaks is isolated to that adapter layer and doesn't ripple through the rest of the system.

Initial and current implementation is a **fIoT (fake IoT) generator** — simulated station/equipment telemetry, following the same generator pattern already adapted from Yearly Yields — specifically so the workflow (generator → adapter/ETL → MCP write tool → SQL Server) can be proven end to end without investing in real hardware under current budget constraints. Real protocol selection is deferred until hardware procurement is actually funded; the adapter seam is what keeps that a swap-in decision later rather than a rework of the ingestion pipeline.

## Auth / RBAC — 2026-07-22
**1.1:** single "developer" SQL login, static connection string, no real auth.

**1.2:** modern ASP.NET Core identity, not the legacy `System.Web` Membership/RoleProvider model (that API isn't available in .NET 10 at all). The equivalent shape in current .NET is `AddAuthentication()`/`AddAuthorization()` registered on the builder alongside the rest of the app's DI/config setup:
- A `Users` table keyed by a stable `UserId` (GUID, distinct from the mutable `Username`), with properly hashed passwords (bcrypt/Argon2id, not a fast general-purpose hash), validated by a dedicated login mutation that takes plaintext credentials over TLS and returns a signed token/cookie carrying role claims. The hash itself never transits GraphQL or any other API surface.
- Role claims from that token drive authorization uniformly at **both** layers: Blazor page/component level (`[Authorize(Roles = "...")]` on pages/components) and the GraphQL/Hot Chocolate layer (`[Authorize(Roles = "...")]` on resolvers/fields). Same principal, same claims, checked at whichever layer is relevant — this is the mechanism that lets a station supervisor's query return a different data shape than a plant manager's without separate endpoints.
- **No live SQL-Server-login fallback.** An earlier version of this plan considered falling back to `TrySqlServerLogin()` with user-submitted credentials when no `Users` row matched. Dropped: forwarding arbitrary user input into a live database authentication attempt turns the login form into a SQL Server credential-testing oracle, and conflates database principals with application user accounts. The escape-hatch need is solved instead by a **seeded developer/break-glass account directly in the `Users` table** (EF Core seed data, hashed like any other row) — same authentication path as everyone else, no second mechanism. That seeded account should be disabled or rotated before any deployment beyond local dev.
- Refresh tokens use rotation **and** family/reuse revocation, not rotation alone — a reused (already-consumed) refresh token invalidates the entire token family, not just that one token.
- **Forward-looking seam for Azure Entra ID:** local `Users`-table identity (1.2) is the on-prem stand-in, not the end state — ASP.NET Core Identity integrates cleanly with Entra ID as an external provider (via `Microsoft.Identity.Web`), and Entra ID's object IDs give stable, traceable principal identifiers that correlate cleanly with Azure-native logging/monitoring (Application Insights/Azure Monitor). Same on-prem-now/Azure-compatible-later shape as SQL Server and Kafka: reserve a nullable `ExternalPrincipalId` (Entra object ID) column on `Users` now, unused until Entra integration happens.

## Operational Resilience — 2026-07-22
- **Backup:** SQL Server 2025 Express has no true HA/failover option (no Always On Availability Groups) — the honest resilience story at this stage is backup-and-restore, not zero-downtime. A scheduled full backup runs during off-hours (the plant isn't 24/7), using **Simple recovery model**. RPO is "since last night's backup." The backup job is a **separate supporting system**, not built into any domain microservice — a standalone scheduled process (own executable, or a Windows Task Scheduler job invoking a backup script) rather than a `BackgroundService` inside the app, so it can be disabled/paused for a demo without touching or restarting the application being demoed. This also sidesteps SQL Server Express not shipping SQL Server Agent.
- **User-action tracing doesn't need to wait on Entra:** `Users.UserId` (stable GUID PK, distinct from the mutable `Username`) is the correlation key for logging/tracing on the on-prem side now — same pattern as `Station.StationId`. The Entra object ID reserved in Auth/RBAC becomes an *additional* correlation dimension once that integration exists, not the only path to tracing.
- **Structured logging/alerting envelope:** one shared shape across all domains rather than per-domain log formats, keyed by the relevant entity's stable id (never a mutable display name) plus a `datetime2` timestamp:
  ```csharp
  public sealed record TelemetryEvent(
      string EntityId,        // the relevant domain entity's stable id (StationId, UserId, etc.)
      string EntityType,      // discriminator: "Station", "User", "SensorReading", ...
      DateTime2 Timestamp,    // when it happened, for ordering/tracing
      string EventType,       // e.g. "Created", "StatusChanged", "ThresholdBreached"
      string? Payload         // domain-specific JSON, opaque to the logging/alerting layer
  );
  ```
  Alerts reuse the same envelope — `EventType` values the Escalation & Notification fan-out subscriber watches for — so that pub/sub pattern can subscribe generically without domain-aware routing logic.

## Data Retention — 2026-07-22
Modeled on the same tiered approach used in Yearly Yields:
- **Detail records** (raw telemetry): kept in full for the **current quarter + previous quarter** — a rolling 2-quarter window.
- **Compaction**: once a quarter ages out of that window, its detail records are compacted down to **one summary row per shift** (aggregated stats — min/max/avg), rather than kept as raw readings.
- **Purge**: summarized data is retained until **one calendar year plus one month** of total age, then removed entirely.
- **Configurability**: the detail-window size (quarters), and total retention duration, are environment-variable-configured rather than hardcoded.

The shift-level summary row this job produces is the natural trigger point for regenerating persisted embeddings once that pipeline exists — same aggregate-upsert-triggers-embedding-regeneration pattern as Yearly Yields' `upsert_historical_summary`, just shift-scoped instead of week-scoped.

## Public API (Phase 5) — 2026-07-22
Not anonymous/open access — "public-facing" means externally reachable, not unauthenticated. Successful login through Identity issues a user-specific signed JWT, and that token is what any external consumer (Phase 5 client, partner integration, etc.) authenticates with — no separate anonymous-access design needed. This pattern is already working in Yearly Yields.

Rate limiting and query cost/depth limits for the GraphQL layer are still worth a pass once Phase 5 is actually being built, but scoped per-authenticated-token rather than as a defense against anonymous traffic.

## Documentation Consistency: README/OVERVIEW Device Roadmap — 2026-08-02
OVERVIEW.md named React Native for the mobile Island while README.md's Island Architecture section named no mobile technology — a real misalignment, since Blazor (desktop/laptop) is already built while mobile isn't. Resolved by treating the two differently rather than picking one label for both:
- **README.md's Roadmap** now spreads device rollout across phases instead of one late mention: tablet in Phase 2 (simulated via scaled browser window ahead of a dedicated build), mobile in Phase 3, watch/AR/TV as their own Islands in Phase 4 — each device family gets exactly the data it needs via GraphQL query shaping, with platform-specific rendering (including OS-level differences like Android vs. iOS) handled inside that device's own Island. No new "ETL adapter" concept needed for this — that term stays reserved for the sensor/PLC ingestion adapter pattern (see Ingestion / Hardware above) so the same word doesn't mean two different things in the same codebase.
- **OVERVIEW.md** was corrected to *not* name a mobile framework — Blazor for desktop/laptop is stated as already built, while mobile's framework is explicitly left undecided (React/React Native is earmarked for the separate Mosaic project, not Telara — an early draft of this fix mistakenly attributed it to Telara and was corrected). This matches the project's general intent to stay tech-agnostic in OVERVIEW.md except where an infrastructural choice is already a committed fact.

## Theory of Constraints Data Dependency — scoped, decision deferred to 1.2 (2026-07-22)
The backlog/surplus diagnostic depends on the station-to-workflow routing graph, which is deferred past Phase 3 prerequisite work — Herbie-finding isn't meaningful until that graph exists.

The test/demo plant will need generators for multiple distinct equipment types (drill press, band saw, planer, plasma cutter, 3D printer, etc. — exact list still open). The workflow model needs to demonstrate three station-level operation patterns: **parallel** equipment operation at a station, **sequential** equipment operation at a station, and a **combination** of both. Whether that's expressed as one long chained workflow with blocks representing each scenario, or as separate independent blocks with unique outputs, is explicitly left open — either is acceptable — and will be finalized once UI work starts in Branch 1.2, after minimal workflows and their target parts/sub-assemblies are picked.

## Business Logic Placement: Centralized Service vs. MCP Tool/Skill — 2026-08-02
As the MCP Server Topology takes shape (Internal Functionality / Haiku / Sonnet servers — see `ARCHITECTURE.md`), a pattern kept surfacing where smaller business-logic workflows were getting absorbed directly into an MCP tool or skill's own behavior rather than staying in the managing service. Decided: **business logic stays centralized in the managing service language** (the .NET domain/service layer for Telara; the equivalent managing service for Mosaic). MCP tools remain thin adapters over existing internal methods — same pattern already established for the Internal Functionality Server and the ingestion ETL adapter (see Ingestion / Hardware above): wrap-and-expose, not reimplement-inside-the-wrapper.

Reasoning:
- **Single source of truth is already the project's working pattern for cross-cutting concerns** — Auth/RBAC checks the same token/claims identically at the Blazor layer and the GraphQL layer rather than duplicating the rule per surface. Splitting business logic across MCP tools vs. the managing service breaks that same pattern and lets the two drift.
- **Tool invocation is non-deterministic** — whether a rule embedded in a tool actually runs depends on whether the LLM decides to call that tool on a given pass. Firm invariants (lease-conflict handling, threshold-breach detection) shouldn't have their execution gated behind model tool-selection behavior.
- **Multiple non-LLM consumers need the same rules** — GraphQL mutations, the compaction job, and the Escalation & Notification fan-out subscriber all need access to the same logic without going through a model.
- **Testability/auditability** — a centralized service method gets a normal unit test; a rule embedded in a tool's behavior is much harder to pin down and verify hasn't regressed, and this project already treats auditability as a hard requirement (see the refresh-token family-revocation reasoning in Auth/RBAC above).

**Trigger to revisit:** this is an architecture default, not a permanent rule. If a specific team (most likely on Mosaic, given its earlier-stage adoption curve) demonstrably can't iterate fast enough going through a shared centralized service — i.e., the bottleneck becomes organizational/velocity, not architectural — that's the actual signal to reconsider decentralizing that team's logic into its own tool/skill, not a general policy shift made in advance of that pain being felt.

## MCP Server Topology — 2026-08-02
Three MCP servers, split by workload shape rather than by domain — each one's job dictates which model (if any) sits behind it:

1. **Internal Functionality Server** — no LLM involved. Provides domain data (relational DB) or data slices (NoSQL DB), parameterized to a set number of inputs, returning only what a given lookup needs — not the full aggregate from root entity through every child/column. Same "consumer gets exactly what it needs" principle GraphQL already applies at the query-field level, applied here at the MCP tool-parameter level. Wraps existing internal .NET methods as tools rather than reimplementing logic inside the tool handler — see Business Logic Placement above: plain method → MCP tool adapter, not logic written directly inside a tool.
2. **Claude Haiku MCP Server** — straightforward linear workflows where throughput and cost matter more than depth of reasoning; targets similar or better latency than the Sonnet server at a lower per-call cost. Works off its own exposed tools/skills to pull data slices into one or more workflows, aiding processing time for business logic running in the system's microservices (logic itself stays centralized per the decision above — these tools/skills are data-slicing and workflow-integration surfaces, not a home for the business rules themselves).
3. **Claude Sonnet MCP Server** — deep-reasoning/high-complexity workflows (the one already being built out in `ARCHITECTURE.md`'s Sprint Goal). The RAG/vector-search pipeline (see AI/RAG above) will most likely live here rather than on Haiku or the internal server, since the collapse-to-relational-fallback reasoning needs the stronger model.

This maps onto README's orchestration diagram (Server A auto-scales under load, Server B idles until a bottleneck hits) but adds the workload-shape distinction: which of these three a given tool belongs to is a cost/latency/reasoning-depth decision, not just a scaling one.

**Physical geography:** all three servers run on one machine through demo-stable/user-testing, since real load testing hasn't happened yet to justify splitting them out. If cloud hosting becomes available, physical location stops mattering as long as the connection between request code, the LLM, and the MCP server can be maintained — same on-prem-now/cloud-compatible-later shape as the rest of this document.

## Database Schema Ownership: EF Core Migrations vs. Raw SQL Scripts — 2026-08-02
`Telara.SQLScripts` originally held both schema (`CREATE TABLE`/`ALTER TABLE`) and seed data as hand-written, numbered raw SQL files. That collided head-on the first time an EF Core migration was generated and applied against the real `telara_ops` database: the migration tried to create `Roles`/`Users`/`RefreshTokens`/`StationEquipmentReadings`, which already existed from the raw scripts, and failed mid-batch (rolled back cleanly, since SQL Server DDL is transactional — no partial damage).

**Decided: schema is owned end-to-end by EF Core Code-First migrations** (`Telara.Domain/Migrations`, applied via `dotnet ef database update`), not hand-written SQL. `Telara.SQLScripts` is now scoped to **seeding only** — demo/reference data, never table creation or alteration. This is a direct expression of the project's own stated goal: one project should be a true .NET stack at its core end-to-end, with other languages/tools brought in deliberately to extend functionality (Java/Spring for Escalation & Notification, potential React Native for a future mobile Island) rather than schema management quietly living outside the stack in raw SQL.

**What this meant in practice:** the pre-existing raw-SQL-created tables held only trivial local dev data (a handful of seed rows, no real users), so rather than write a migration to reconcile against hand-written DDL, the tables were dropped and recreated fresh from the EF migration. `Telara.SQLScripts`' schema-creation files (`000`–`002`, `006`–`007`) were deleted as superseded; the two seed-data scripts (`SeedRolesTable`, `SeedUsersTable-Demo`) were kept and renumbered (`000`, `001`) since seeding remains their job and their target column shapes already matched the new EF-generated schema unchanged.

**Also fixed while migrating:** the EF model's `StationEquipment` → `EquipmentType` foreign key defaulted to `ReferentialAction.Cascade` (EF Core's default for a required FK). Left as-is, deleting one `EquipmentTypes` reference row (e.g., "Drill Press") would have cascade-deleted every piece of equipment of that type and, transitively, all its `SensorReadings`. Changed to `DeleteBehavior.Restrict` — a lookup-table row can't be deleted while anything still references it.

**Still open, not resolved here:** exact tool inventory per server — which specific lookups/workflows belong to which of the three — is deferred until MCP implementation actually starts in Branch 1.2.

## Employee Domain Model & StationEquipment Shape — planned for 1.4/1.5 (2026-09-17)
Not implemented yet — this is a forward-looking scope note, written now so it isn't rediscovered from scratch when the Employee model actually gets picked up.

The Station Supervisor dashboard prototype's "Staffing · this shift" panel and "Active alerts" panel both depend on domain concepts that don't exist yet: no `Alert` entity, no `Employee`/staffing entity, and no assignment of an Employee to a `StationEquipment` slot. Building that out surfaced a naming question: not every `StationEquipment` row is a powered machine — some (e.g. "Packing," "Labeling" in the prototype) are manual work areas where a human performs an action by hand, with no sensor telemetry at all.

**Resolved: `StationEquipment` stays one entity, not split into a separate `WorkArea` concept**, since its columns (`LastSensorReadingUtc` etc.) are already nullable and tolerate a manual slot simply never accruing sensor data — same table, same `StationSequence`/lease/status machinery either way. This also means `Employee`-to-slot assignment (for staffing tracking and plant-hierarchy/location tracking) targets `StationEquipment` uniformly, automated or not.

**Planned natural iteration once manual-vs-automated needs more than a nullable field or two:** split via the interface pattern already established for other domain models (`Station : IStation`) — `ManualStationEquipment`, `ControlledStationEquipment`, and `AutomatedStationEquipment` all implementing `IStationEquipment`, rather than widening one class indefinitely to hold every variant's properties. Not committed now; flagged so the single-entity choice isn't mistaken for permanent when that need shows up.

**Dispatch as an `Alert` trigger source, not just a diagram box (2026-09-17 addendum):** every station has its own Dispatch — the outbound staging area past its last equipment step — regardless of whether that station happens to be the whole line's `IsLoadingDock` terminus. Once product leaves a station's last equipment step, it's Dispatch's problem, *unless* Dispatch stops picking up and the station's outbound area fills up, forcing the station to idle. That backpressure condition (Dispatch not clearing → outbound fills → station idles) is the same shape as the prototype's "Packing idle 10+ min, waiting on Riveting" alert and is exactly the kind of condition the `Alert` entity needs to detect once it exists — not a new domain concept, just a concrete example to design the `Alert` trigger vocabulary against when that work starts.

## Single-Line Station Routing — 2026-09-01
`Station.NextStationId` (string?, self-referencing FK within the same `FacilityId`) and `Station.IsLoadingDock` (bool) give 1.3's workflow diagram screen real topology to render, chosen over introducing a new `StationRoute`/`Assembly` entity to keep the addition minimal and reversible. `NextStationId` null means the station is a terminal node; linear only — one outgoing edge per station, no branching/merging. This is the station-to-workflow dependency graph flagged as an explicit non-goal since 1.2, scoped down here to a single line's linear path rather than the full multi-line/cross-plant graph Phase 5 eventually needs. A richer model can replace this once cross-line Sonnet analysis (1.4+) defines what it actually needs.

`GetStationWorkflow(facilityId, stationId)` — the GraphQL query walking the `NextStationId` chain to its `IsLoadingDock` terminus — reads go through direct MediatR, not MAF, same reasoning as `GetSensorReadings` in 1.2: bulk UI-shaping reads for high-frequency/UI-driven access don't need MAF's routing hop, which stays reserved for the ingestion/registration flows it already owns.

**RegisterStation predecessor linking and fan-out boundary — 2026-09-15 addendum:** `RegisterStation` accepts `predecessorStationIds` (a list) and `isLoadingDock`, closing the gap where routing previously had no mutation and was stitched by hand via raw SQL. The list shape supports funneling multiple existing predecessors into one new successor — each predecessor just gets its own `NextStationId` pointed at the new station, which the existing single-FK schema already allows, without a breaking API change once the registration form offers multi-predecessor selection. **Fan-out (one station feeding more than one successor) is explicitly unsupported** — confirmed as a deliberate boundary, not a gap. `NextStationId` stays a single outgoing edge per station; if a real fan-out need shows up, it's modeled as a distinct Station variant/type that owns the one-to-many split as its own concern, not a generalization of every station's routing.

## Role-Based View Reuse (Drill-Down Hierarchy) — 2026-09-17
A role's dashboard is not necessarily a distinct screen — it's whichever view is that role's default/most-used landing, and the left nav can point a role at a *shared* view owned by the role below it rather than a scaled-down copy. Concretely: a Shift Manager's default landing is the line-level view (stations as a single unit), but the left nav also exposes the same per-station view Station Supervisors land on by default, reused as-is for drill-down — not a separate Shift-Manager-flavored station screen.

Reasoning: a Shift Manager coordinating a resolution with a Station Supervisor needs to see the *same* station-level picture the Supervisor is looking at (equipment status, active alerts, staffing), not a summary of it — the Manager's actual value-add at that point is resources the Supervisor can't reach (e.g. calling in outside staff on overtime), not a different data view. This directly confirms the broader principle (reaffirmed 2026-09-24): every role should be looking at the *same* underlying plant data, with UI component views specific to their role rather than separately summarized/gatekept data per role.

This reuse-down-the-hierarchy pattern is expected to extend to Plant Director → Shift Manager drill-down once that work starts (1.4/1.5) — a Director drilling into a line for the same reason a Manager drills into a station: coordinating with the role below on a resolution, not viewing separately-built Director-flavored line data. Which view is a role's *default* landing stays an open, revisitable choice per role (not architecturally fixed) — deliberately left open for post-launch UX feedback rather than decided now.

## Role-Restricted Write Mutations — 2026-09-24
Reads (dashboards, workflow diagram, station/equipment lookups) stay `[Authorize]`-only, open to all three authenticated roles, per the Role-Based View Reuse principle above — every role sees the same underlying data. Only the two write mutations that create/modify plant topology, `RegisterStation` and `RegisterStationEquipment`, are role-restricted to `[Authorize(Roles = ["Station Supervisor"])]` — station/equipment registration is a floor-level operational duty, not something Shift Manager or Plant Director perform, even though they can see the results.

This was the first `[Authorize(Roles = ...)]` restriction added to the codebase — prior to this, every `[Authorize]` attribute (GraphQL resolvers and Blazor `@attribute [Authorize]`) was authentication-only, with no role gating anywhere despite `Users.Role`/the JWT's role claim having existed since 1.1/1.2's Auth/RBAC work. Verifying this actually works required a new test harness (`Telara.OpsApi.Tests/GraphQL/AuthorizationTests.cs`) that executes through a real HotChocolate `IRequestExecutor`, since every prior test called resolver static methods directly — bypassing `[Authorize]`/`[Authorize(Roles = ...)]` entirely, since that's pipeline middleware around resolver execution, not resolver-body logic. Same pattern is intended to carry over to Mosaic's own HotChocolate authorization testing when that comes up.

## Station Routing Integrity: DB Constraints vs. Scheduled Validation — 2026-09-24
`RegisterStationCommandHandler` validates predecessor-linking (no successor already claimed, no linking onto a loading dock — see Single-Line Station Routing above), but `Telara.SQLScripts` writes `next_station_id`/`is_loading_dock` directly via raw SQL, bypassing that validation entirely — this is how the original demo line was stitched before `RegisterStation` supported predecessors, and remains the likely seeding path for future demo lines. Two gaps this creates were closed/scoped differently based on how cheaply each can be enforced:

**Self-reference and the loading-dock contradiction — closed now via DB check constraints** (same migration as this decision's date): `CK_Stations_NextStationId_NotSelf` (`next_station_id IS NULL OR next_station_id <> station_id`) and `CK_Stations_LoadingDock_NoSuccessor` (`NOT (is_loading_dock = 1 AND next_station_id IS NOT NULL)`) on `dbo.Stations`. Cheap, unconditional, and catch a raw-SQL mistake regardless of which script or hand-written statement makes it — the right shape for an invariant that should never be true no matter the write path.

**Longer cycles (A→B→C→A with no station ever marked `IsLoadingDock`) — not a DB constraint, deferred to developer discipline plus a future scheduled validation job**, since a multi-hop cycle isn't expressible as a single-table CHECK constraint (would need a recursive CTE or trigger, disproportionate for a demo-seeding-only risk). **Planned, not built:** a standalone scheduled process (same "separate supporting system, not a `BackgroundService` inside the app" pattern as the backup job in Operational Resilience above) walks every registered line's `NextStationId` chain looking for a cycle or a dead end, once a day, against a configured list of lines to monitor. The chain-walk/cycle-detection itself must stay plain deterministic C# — this is exactly the kind of firm invariant the Business Logic Placement decision above already argues against gating behind LLM tool-selection. Where an LLM (Haiku, per its data-slicing/workflow-integration role in the MCP Server Topology decision) fits is downstream of the check: turning the deterministic findings into a plain-English SendGrid email body, not performing the detection itself.
